这样，四个cli就聚齐了。它使用的几乎也是和claude code一样的配置，也就是三大提供。
包括ucopilot，codex和阿里云。
为数不多的可以官方支持多家的模型，支持多方api的cli。

由于可以使用glm5.1和qwen3.6plus，所以我也使用了35来订阅一下（但是感觉完全不够用，一个月的用量大概只能用于5-10个项目左右（以内）。）
所以也不用想着把它给其他的东西了哈。

opencode GO的额度从官网看就行。其他三个运营的话从其他界面查看。