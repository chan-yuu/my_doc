1.spacesniffer
可视化磁盘占用

学习了如何默认管理员启动：
- 找到程序的 **主程序文件（.exe）**（不是快捷方式）
- 右键 → **属性** → 切换到 **兼容性** 选项卡
- 勾选：**以管理员身份运行此程序**
- （可选）点 **更改所有用户的设置** → 再勾一遍，让所有用户都生效
- **确定 → 应用**

怎么让系统能够搜到你的应用：
```
shell:programs
```
添加一个快捷方式进去就好了。
